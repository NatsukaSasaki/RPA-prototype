## playwright
Microsoftが開発したオープンソースのWebブラウザ操作・テスト自動化フレームワーク。ブラウザ制御を自動化することができる。

Playwrightではロケーターの機能を使って要素を検索します。

|意味|ロケーター |
|:-------|:-------|
|画面遷移|page1.goto("URL")|
||locator("input[type=\"*[]\"][value=\"[]\"]")|
||locator("***").click()|
||page.getBy***("***").click|
|||

### getByRole
ARIAロール、ARIA属性、アクセス可能な名前で要素を見つけることができます。
await page.getByRole('chaekbok',{name: 'SUb'})


## ApScheduler
Pythonコードや関数を指定した日時や定期的に自動実行させるための強力なPythonライブラリ

|コンポーネント |役割|概要|
|:-------|:-------|:-------|
|トリガー（Triggers）|`スケジュールロジック`|ジョブをいつ実行するか決定します。初期設定後はステートレスです。|
|ジョブストア（jobStore）|`ジョブの保管場所`|スケジュールされたジョブを格納しますデフォルトはメモリですが、DBにも保存できます。|
|エクゼキュータ（Executors）|`ジョブの実行担当`|ジョブ内の指定された呼び出し可能オブジェクト（関数など）を通常はスレッドプールやプロセスプールに投入して実行します。実行完了後、スケジューラに通知します。|
|スケジューラ（Schedulers）|`全体の統合・管理`|ほかのコンポーネントを結び付け、ジョブの追加、変更、削除などのインターフェースを提供します。通常、アプリケーション内で１つのスケジューラを実行します。|


|意味|コマンド |
|:-------|:-------|
|ファイル名内の関数を呼び出し|from ファイル名 import 関数|
|スケジューラの初期化|BlockingScheduler(timezone='Asia/Tokyo')#タイムゾーン指定|
|ジョブの追加|scheduler.add_job()|
||``|
||``|


## openpyel

Excelを操作するためのPythonライブラリ

|意味|コマンド |
|:-------|:-------|
|ワークブック作成|`excel=openpyxl.Workbook()`|
|捜査対象のシートを取得|`sheet=wxcel['シート名']`|
|値の入力|`sheet.cell("行","列","セルへ入力したい値")`|
||``|
||``|
||``|
